// import React from "react";

// type ToastMessage = {
//   message: string;
//   type: "SUCCESS" | "ERROR";
// };

// type AppContextType = {
//   showToast: (toastMessage: ToastMessage) => void;
// };

// export const AppContext = React.createContext<AppContextType | undefined>(undefined);

// export const AppContentProvider = ({
//   children,
// }: {
//   children: React.ReactNode;
// }) => {
//   return ( 
//     <AppContext.> 
    
//   );
// };

